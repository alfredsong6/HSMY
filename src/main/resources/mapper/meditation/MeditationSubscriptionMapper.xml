<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hsmy.mapper.meditation.MeditationSubscriptionMapper">

    <select id="selectLatestByUser" resultType="com.hsmy.entity.meditation.MeditationSubscription">
        SELECT *
        FROM t_meditation_subscription
        WHERE user_id = #{userId}
          AND is_deleted = 0
        ORDER BY end_time DESC
        LIMIT 1
    </select>

    <select id="selectActiveByUser" resultType="com.hsmy.entity.meditation.MeditationSubscription">
        SELECT *
        FROM t_meditation_subscription
        WHERE user_id = #{userId}
          AND end_time >= #{now}
          AND status = 'CURRENT'
          AND is_deleted = 0
        ORDER BY end_time DESC
        LIMIT 1
    </select>

    <update id="expireSubscriptions">
        UPDATE t_meditation_subscription
        SET status = 'EXPIRED',
            update_time = NOW()
        WHERE status = 'CURRENT'
          AND end_time < #{now}
          AND is_deleted = 0
    </update>

</mapper>
