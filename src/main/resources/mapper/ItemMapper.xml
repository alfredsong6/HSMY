<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hsmy.mapper.ItemMapper">
    
    <!-- 基础结果映射 -->
    <resultMap id="BaseResultMap" type="com.hsmy.entity.Item">
        <id column="id" property="id"/>
        <result column="item_name" property="itemName"/>
        <result column="item_type" property="itemType"/>
        <result column="category" property="category"/>
        <result column="price" property="price"/>
        <result column="original_price" property="originalPrice"/>
        <result column="description" property="description"/>
        <result column="preview_url" property="previewUrl"/>
        <result column="resource_url" property="resourceUrl"/>
        <result column="is_limited" property="isLimited"/>
        <result column="limit_time_start" property="limitTimeStart"/>
        <result column="limit_time_end" property="limitTimeEnd"/>
        <result column="stock" property="stock"/>
        <result column="sold_count" property="soldCount"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="is_active" property="isActive"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>
    
    <!-- 基础字段列表 -->
    <sql id="Base_Column_List">
        id, item_name, item_type, category, price, original_price, description,
        preview_url, resource_url, is_limited, limit_time_start, limit_time_end,
        stock, sold_count, sort_order, is_active, create_by, create_time, update_by, update_time, is_deleted
    </sql>
    
    <!-- 根据道具类型查询道具列表 -->
    <select id="selectByType" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_item
        WHERE item_type = #{itemType}
        AND is_active = 1
        AND is_deleted = 0
        ORDER BY sort_order ASC, create_time DESC
    </select>
    
    <!-- 查询所有上架的道具 -->
    <select id="selectAllActive" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_item
        WHERE is_active = 1
        AND is_deleted = 0
        ORDER BY sort_order ASC, create_time DESC
    </select>
    
    <!-- 根据分类查询道具 -->
    <select id="selectByCategory" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_item
        WHERE category = #{category}
        AND is_active = 1
        AND is_deleted = 0
        ORDER BY sort_order ASC, create_time DESC
    </select>
    
    <!-- 查询限时道具 -->
    <select id="selectLimitedItems" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_item
        WHERE is_limited = 1
        AND limit_time_start &lt;= NOW()
        AND limit_time_end >= NOW()
        AND is_active = 1
        AND is_deleted = 0
        ORDER BY sort_order ASC, limit_time_end ASC
    </select>
    
    <!-- 更新道具销量 -->
    <update id="updateSoldCount">
        UPDATE t_item
        SET sold_count = sold_count + #{quantity},
            update_time = NOW()
        WHERE id = #{itemId}
        AND is_deleted = 0
    </update>
    
</mapper>